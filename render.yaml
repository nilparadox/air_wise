services:
  - type: web
    name: air-wise
    runtime: python
    buildCommand: pip install -r requirements.txt && python scripts/train_digital_model.py
    startCommand: uvicorn app.api.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
